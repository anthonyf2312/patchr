/** Discord allows 4,000 characters of text across all text components in one message. */
export const MESSAGE_TEXT_LIMIT = 4000;

/** Longest body a note can have. The rest of the 4,000 is kept for the header, footer and ping. */
export const NOTES_MAX_LENGTH = 3500;

export const VERSION_MAX_LENGTH = 32;
export const TITLE_MAX_LENGTH = 100;

/** Patchr pink (#FF5E8A), the accent on every post. */
export const BRAND_COLOR = 0xff5e8a;
